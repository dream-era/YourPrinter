{
  "name": "printq-print-agent",
  "version": "1.0.0",
  "description": "Background service that polls YourPrinter for queued print jobs and sends them straight to the shop's default printer — no manual OS print dialog.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pdf-to-printer": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
